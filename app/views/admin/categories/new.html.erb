<div class="admin-header">
  <h1><%= category.persisted? ? "Editar Categoría" : "Nueva Categoría" %></h1>
</div>

<div class="admin-nav-strip">
  <%= link_to "← Categorías", admin_categories_path %>
</div>

<%= render "form", category: category %>
